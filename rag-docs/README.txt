Place your documents here for RAG (Retrieval-Augmented Generation).

Supported formats include .txt, .pdf, and .md files.

On first use (when you prefix a message with "rag"), LlamaIndex will embed these
documents and save the vector index to ../storage/ for faster subsequent loads.
